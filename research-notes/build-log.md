2026-05-11 — asteroids-rotate-thrust — LOC: 360 — tech: canvas-2d — notes: vanilla rAF, semi-implicit Euler with exp(-k·dt) friction, torus-aware collisions, wrap-ghost rendering; touch zones (L-third/R-third/bottom-pad/top-right-FAB).
2026-05-11 — qix-area-claim — LOC: 399 — tech: canvas-2d — notes: 200x150 Uint8Array grid, marker substepped 1-cell-per-tick, Gendel single 4-flood from Qix on close, degenerate-trail (<3 cells) treated as death, segment-rasterised trail-kill via Bresenham, putImageData blit for grid render, D-pad + DRAW + R touch UI.
2026-05-11 — baba-is-you-rewrite — LOC: 391 — tech: canvas-2d — notes: 12x9 grid; turn-based step(dx,dy) atomic: snapshot → push-chain from each YOU (top-left order, two-pass safe) → parseRules() scans both axes for SUBJECT-IS-PROP triples → checkWin shared-cell YOU+WIN; PUSH-wins-STOP precedence documented in isStop; NOUN-IS-NOUN substitution out of scope; 50-step undo via per-tile {kind,x,y} snapshots; ease-out cubic slide tween (skipped under reduced-motion); swipe + UNDO/RST FABs; seed level solvable by pushing W_STOP at (3,7) DOWN to break WALL IS STOP, then walking through the now-walkable wall fortress to FLAG at (8,4).
2026-05-11 — defender-of-the-crown-catapult — LOC: 534 — tech: canvas-2d — notes: logical 480x270, GROUND_Y=240, g=320 u/s²; launch decomposes vx=power·cos(θ) / vy=-power·sin(θ); semi-implicit Euler substepped ceil(|v|·dt/4) so 14×10 brick AABBs don't tunnel at P=140; 6×8 brick grid (48) anchored at WALL_X=360, win threshold ≤24 remaining; WIN-before-LOSE in endShot so final-stone breach counts; 12 Hz dt-accumulator hold-repeat on UP/DOWN angle ±1°, LEFT/RIGHT power ±1; Space edge-triggered via spaceHeld latch (no auto-fire on resolve); design choice: brick hit stops projectile with single-frame #ff5e3a flash + dust puff (no bounce — visually clearer than ricochet for the verb); touch: top horizontal angle slider, right vertical power slider, bottom-center FIRE FAB, top-right R FAB, logical-px coords reverse-mapped through stage scale; held-flag clear on blur to prevent stuck-arrow drift; reduced-motion suppresses arm-recoil tween and hit-flash, parabola preserved.
2026-05-11 — lunar-lander-thrust — LOC: 351 — tech: canvas-2d — notes: logical 480x320, G=18 / T_MAG=46 (TWR ≈ 2.55) / BURN_RATE=22 / ROT_RATE=2.6, V_SAFE_X=14 / V_SAFE_Y=22 / TILT_SAFE=0.18 rad; semi-implicit Euler (v then x) under constant gravity; thrust decomposes ax=sin θ·T, ay=-cos θ·T; terrain is one N=31-vertex midpoint-displacement polyline driven by a fixed-seed LCG ('LAND') so R rehearses the same problem per spec — flat pad stamped at deterministic column ~9–18, width 80 px; terrainAt(x) does linear interp between columns and substep is unnecessary because dt is clamped at 50 ms and the ridge is C0-continuous; fuel-out behaviour: engine gates on fuel>0 so an empty tank means free fall — the terminal vy hits V_SAFE_Y unless the player has remaining fuel, which is the spec's "deferred cost" payoff; on contact freeze vx=vy=0 to suppress jitter (per spec) and gate tick() on status==='play'; touch zones (left third / right third / bottom-center pad) drawn as faint #222 outlines only while a touch is on stage, two-finger tap = restart; held-input flags cleared on blur and on visibilitychange so a stuck ArrowUp doesn't auto-thrust on resume; prefers-reduced-motion suppresses flame flicker and crash flash, lander motion preserved.
2026-05-12 — boulder-dash-falling-rocks — LOC: 405 — tech: canvas-2d — notes: 20x14 Uint8Array grid + parallel falling[] flag; per-keypress turn: tryMove (push only horizontal, EXIT iff collected≥QUOTA=4) then scanPhysics() iterated to fixed point with 200-pass guard; bottom-up (y descending) sweep with scanned[] bitmap so each tile moves ≤1 cell per pass — central BD invariant: a rock that just fell can't fall again in same pass, otherwise toppling rocks skip the player one row down; topple checks (x+s,y)=EMPTY AND (x+s,y+1)=EMPTY so toppled rock can resume falling next pass without infinite-loop on adjacent rocks; falling→rest transition marks changed=true so next pass evaluates topple on round surfaces; push clears destination's FALLING flag; hand-authored seed: 3-rock stack at col 13 rows 3..5 above keystone dirt row 6 above diamond at (13,7), 4 reachable diamonds incl. (14,1), (13,7), (11,10), (4,12), 2 unreachable in sealed chamber at (5..6,8), push-rock at (9,10) into empty pocket at (10,10); verified solvable in 52 moves via path (14,1)→(15,*)→(15,6)→(14,6)→(13,6) dig keystone→(14,6) trigger cascade→(14,7)→(13,7) collect→(12,7) escape (cascade chains across 5 passes including sideways topple to (14,7))→(12,*)→(11,10)→(*, 12)→(4,12)→(18,12); naive col-14 descent dies at row 5 to toppled-then-fallen rock — kill-rule demo confirmed via vm sim; 0.08s ease-out tween snapped to instant under prefers-reduced-motion; swipe-to-step + RST FAB touch UI; pulse on EXIT once quota met, suppressed under reduced-motion.
